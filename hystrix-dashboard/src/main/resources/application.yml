# 服务端口
server:
  port: 8501

# 应用名称
spring:
  application:
    name: hystrix-dashboard

eureka:
  client:
    # 将服务注册到注册中心
    register-with-eureka: true
    # 获取注册中心服务
    fetch-registry: true
    service-url:
      # 配置注册中心地址
      defaultZone: http://localhost:8081/eureka
